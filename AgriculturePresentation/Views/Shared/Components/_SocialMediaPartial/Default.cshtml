@using EntityLayer.Concrete
@model List<SocialMedia>
<div class="w3l_header_right">
    <div class="w3ls-social-icons text-left">
        @foreach (var x in Model)
        {
            <a class="@x.Title" href="@x.Url"><i class="@x.Icon"></i></a>
      
        }

    </div>
</div>